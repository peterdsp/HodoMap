# Web application

This directory will contain the public TypeScript and React Web application.

The Web client remains web-native for:

- Semantic and indexable operator, route and station pages.
- Keyboard and screen-reader accessibility.
- Link previews and structured metadata.
- Mature browser mapping.
- Fast first content.

It consumes versioned OpenAPI and JSON schema contracts from `data/schemas/`.
